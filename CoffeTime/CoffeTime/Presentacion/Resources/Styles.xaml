<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================
         COLORES PRINCIPALES
    ============================ -->
    <Color x:Key="PrimaryColor">#D88A1A</Color>
    <!-- Naranja café -->
    <Color x:Key="PrimaryDark">#BF7812</Color>
    <Color x:Key="BackgroundColor">#FAF8F2</Color>
    <!-- Fondo general -->
    <Color x:Key="CardColor">#FFFFFF</Color>
    <Color x:Key="BorderColor">#E2D9C6</Color>
    <Color x:Key="TextDark">#4A3E2A</Color>
    <Color x:Key="TextLight">#7A6B56</Color>

    <!-- ============================
         BROCHAS
    ============================ -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDark}" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextDarkBrush" Color="{StaticResource TextDark}" />
    <SolidColorBrush x:Key="TextLightBrush" Color="{StaticResource TextLight}" />


    <!-- ============================
         FUENTES
    ============================ -->
    <Style x:Key="TitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
    </Style>

    <Style x:Key="SubtitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
    </Style>


    <!-- ============================
         BOTONES PRINCIPALES
    ============================ -->
    <Style x:Key="MainButton" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>


    <!-- ============================
         TEXTBOX MODERNO
    ============================ -->
    <Style x:Key="ModernTextBox" TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer Margin="2" x:Name="PART_ContentHost"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ============================
         CARD (TARJETAS)
    ============================ -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#C8B89A" BlurRadius="8" ShadowDepth="2"/>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ============================
         BADGES (roles y estados)
    ============================ -->
    <Style x:Key="Badge" TargetType="Border">
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="BadgeAdmin" TargetType="Border" BasedOn="{StaticResource Badge}">
        <Setter Property="Background" Value="#F4D7FF"/>
        <Setter Property="BorderBrush" Value="#DA9BFF"/>
    </Style>

    <Style x:Key="BadgeOperario" TargetType="Border" BasedOn="{StaticResource Badge}">
        <Setter Property="Background" Value="#DDF1FF"/>
        <Setter Property="BorderBrush" Value="#A9D8F8"/>
    </Style>


    <!-- ============================
         DATAGRID MODERNO
    ============================ -->
    <Style x:Key="ModernDataGrid" TargetType="DataGrid">
        <Setter Property="RowHeight" Value="45"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#E0D8C9"/>
        <Setter Property="VerticalGridLinesBrush" Value="#E0D8C9"/>
        <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
    </Style>

</ResourceDictionary>
