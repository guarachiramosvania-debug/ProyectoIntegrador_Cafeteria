<Window x:Class="CoffeTime.Presentacion.Views.UsuariosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Usuarios" Height="600" Width="900"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Style="{StaticResource TitleText}" Text="Usuarios"/>
            <TextBlock Style="{StaticResource SubtitleText}" Text="Administra los usuarios del sistema" Margin="10,4,0,0"/>
        </StackPanel>

        <Button Style="{StaticResource MainButton}"
                Grid.Row="0"
                HorizontalAlignment="Right"
                Padding="12,6"
                Click="BtnNuevo_Click">
            + Nuevo Usuario
        </Button>

        <Border Style="{StaticResource CardStyle}" Grid.Row="1" Margin="0,15,0,0">
            <DataGrid x:Name="DataGridUsuarios"
                      Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      SelectionUnit="FullRow">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FFF3D4"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                        <Setter Property="Padding" Value="10,5"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Usuario" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="👤" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding NombreUsuario}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Nombre" Binding="{Binding NombreCompleto}" Width="2*"/>

                    <DataGridTemplateColumn Header="Rol" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border x:Name="BadgeRol" Padding="6,3" CornerRadius="10" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Rol}" FontWeight="SemiBold" FontSize="13"/>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Rol}" Value="Administrador">
                                        <Setter TargetName="BadgeRol" Property="Background" Value="#F4D7FF"/>
                                        <Setter TargetName="BadgeRol" Property="BorderBrush" Value="#DA9BFF"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Rol}" Value="Operario">
                                        <Setter TargetName="BadgeRol" Property="Background" Value="#DDF1FF"/>
                                        <Setter TargetName="BadgeRol" Property="BorderBrush" Value="#A9D8F8"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Estado" Binding="{Binding EstadoTexto}" Width="*"/>

                    <DataGridTemplateColumn Header="Acciones" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" Width="30" Height="30" Margin="3" Click="BtnEditar_Click"/>
                                    <Button Content="🗑️" Width="30" Height="30" Margin="3" Click="BtnEliminar_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>